<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.novel.mapper.NovelReviewRecordMapper">

	<resultMap type="NovelReviewRecord" id="NovelReviewRecordResult">
		<id     property="recordId"      column="record_id"      />
		<result property="targetType"    column="target_type"    />
		<result property="targetId"      column="target_id"      />
		<result property="versionId"     column="version_id"     />
		<result property="reviewerId"    column="reviewer_id"    />
		<result property="reviewResult"  column="review_result"  />
		<result property="reviewComment" column="review_comment" />
		<result property="createBy"      column="create_by"      />
		<result property="createTime"    column="create_time"    />
	</resultMap>

	<insert id="insertRecord" parameterType="NovelReviewRecord" useGeneratedKeys="true" keyProperty="recordId">
		insert into novel_review_record(
			target_type, target_id, version_id, reviewer_id, review_result, review_comment, create_by, create_time
		)values(
			#{targetType}, #{targetId}, #{versionId}, #{reviewerId}, #{reviewResult}, #{reviewComment}, #{createBy}, sysdate()
		)
	</insert>

	<select id="selectByTarget" resultMap="NovelReviewRecordResult">
		select record_id, target_type, target_id, version_id, reviewer_id, review_result, review_comment, create_by, create_time
		from novel_review_record
		where target_type = #{targetType} and target_id = #{targetId}
		order by record_id desc
	</select>

</mapper>
