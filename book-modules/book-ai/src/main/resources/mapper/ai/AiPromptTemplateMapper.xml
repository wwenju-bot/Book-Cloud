<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.ai.mapper.AiPromptTemplateMapper">

	<resultMap type="AiPromptTemplate" id="AiPromptTemplateResult">
		<id     property="templateId"   column="template_id"   />
		<result property="templateKey"  column="template_key"  />
		<result property="scenario"     column="scenario"      />
		<result property="content"      column="content"       />
		<result property="version"      column="version"       />
		<result property="enabled"      column="enabled"       />
		<result property="createBy"     column="create_by"     />
		<result property="createTime"   column="create_time"   />
		<result property="updateBy"     column="update_by"     />
		<result property="updateTime"   column="update_time"   />
		<result property="remark"       column="remark"        />
	</resultMap>

	<select id="selectLatestEnabledByScenario" parameterType="String" resultMap="AiPromptTemplateResult">
		select template_id, template_key, scenario, content, version, enabled,
			create_by, create_time, update_by, update_time, remark
		from ai_prompt_template
		where scenario = #{scenario} and enabled = '1'
		order by version desc
		limit 1
	</select>

</mapper>
