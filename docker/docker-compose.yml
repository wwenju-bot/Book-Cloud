version : '3.8'
services:
  rail-nacos:
    container_name: rail-nacos
    image: nacos/nacos-server:v3.0.2
    build:
      context: ./nacos
    environment:
      - MODE=standalone
      - NACOS_AUTH_TOKEN=your_auth_token
      - NACOS_AUTH_IDENTITY_KEY=your_identity_key
      - NACOS_AUTH_IDENTITY_VALUE=your_identity_value
    volumes:
      - ./nacos/logs/:/home/nacos/logs
      - ./nacos/conf/application.properties:/home/nacos/conf/application.properties
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    depends_on:
      - rail-mysql
  rail-mysql:
    container_name: rail-mysql
    image: mysql:5.7
    build:
      context: ./mysql
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/conf:/etc/mysql/conf.d
      - ./mysql/logs:/logs
      - ./mysql/data:/var/lib/mysql
    command: [
          'mysqld',
          '--innodb-buffer-pool-size=80M',
          '--character-set-server=utf8mb4',
          '--collation-server=utf8mb4_unicode_ci',
          '--default-time-zone=+8:00',
          '--lower-case-table-names=1'
        ]
    environment:
      MYSQL_DATABASE: 'rail-cloud'
      MYSQL_ROOT_PASSWORD: password
  rail-redis:
    container_name: rail-redis
    image: redis
    build:
      context: ./redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis/conf/redis.conf:/home/rail/redis/redis.conf
      - ./redis/data:/data
    command: redis-server /home/rail/redis/redis.conf
  rail-nginx:
    container_name: rail-nginx
    image: nginx
    build:
      context: ./nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/html/dist:/home/rail/projects/rail-ui
      - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/logs:/var/log/nginx
      - ./nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - rail-gateway
    links:
      - rail-gateway
  rail-gateway:
    container_name: rail-gateway
    build:
      context: ./rail/gateway
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - rail-redis
    links:
      - rail-redis
  rail-auth:
    container_name: rail-auth
    build:
      context: ./rail/auth
      dockerfile: dockerfile
    ports:
      - "9200:9200"
    depends_on:
      - rail-redis
    links:
      - rail-redis
  rail-modules-system:
    container_name: rail-modules-system
    build:
      context: ./rail/modules/system
      dockerfile: dockerfile
    ports:
      - "9201:9201"
    depends_on:
      - rail-redis
      - rail-mysql
    links:
      - rail-redis
      - rail-mysql
  rail-modules-gen:
    container_name: rail-modules-gen
    build:
      context: ./rail/modules/gen
      dockerfile: dockerfile
    ports:
      - "9202:9202"
    depends_on:
      - rail-mysql
    links:
      - rail-mysql
  rail-modules-job:
    container_name: rail-modules-job
    build:
      context: ./rail/modules/job
      dockerfile: dockerfile
    ports:
      - "9203:9203"
    depends_on:
      - rail-mysql
    links:
      - rail-mysql
  rail-modules-file:
    container_name: rail-modules-file
    build:
      context: ./rail/modules/file
      dockerfile: dockerfile
    ports:
      - "9300:9300"
    volumes:
    - ./rail/uploadPath:/home/rail/uploadPath
  rail-visual-monitor:
    container_name: rail-visual-monitor
    build:
      context: ./rail/visual/monitor
      dockerfile: dockerfile
    ports:
      - "9100:9100"
